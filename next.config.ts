import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactCompiler: true,
  // Hide the Next.js dev indicator button/badge in development
  devIndicators: false,
};

export default nextConfig;
